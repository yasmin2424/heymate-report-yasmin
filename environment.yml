# install with conda env create -f environment.yml 
name: 'heymate-mds-data-clean-pipeline'
channels:
  - conda-forge
  - pytorch
  - anaconda
  - plotly
  - defaults
dependencies:
  - python=3.10
  - pandas
  - numpy
  - requests
  - ipykernel
  - jupyterlab
  - matplotlib
  - seaborn
  - sqlalchemy
  - tqdm
  - ipython
  - pip
  - pip:
      - openai
      - python-dotenv
      - urllib3
      - azure-functions